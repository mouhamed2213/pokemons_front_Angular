import { BorderCard } from './border-card';

describe('BorderCard', () => {
  it('should create an instance', () => {
    const directive = new BorderCard();
    expect(directive).toBeTruthy();
  });
});
